Face detector with Firebase ML Kit

Demo
1. create a new project
2. add project to firebase project: Tools > firebase > RealtimeDatabase > Save and retriece data
   > Connect your app to Firebase > Connect to Firebase >
3. add meda-data to Android Manifest
4. write helper class GraphicOverlay
   this GraphicOverlay will let us draw on the screen. RectOverlay will draw rect aroudn faces
6. change from "RC or Beta" appCompat to lower version will fix error not display perview layout
7. try to test